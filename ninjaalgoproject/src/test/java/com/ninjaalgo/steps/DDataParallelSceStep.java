/*Code details:
	#Author: Meenakshi Dated: 7-Nov-2023
*/
package com.ninjaalgo.steps;

import java.time.Duration;

import org.openqa.selenium.WebDriver;
import org.openqa.selenium.support.PageFactory;

import com.ninjaalgo.driverfactory.DriverFactory;
import com.ninjaalgo.pages.DataStructurePage;
import com.ninjaalgo.pages.HomePage;
import com.ninjaalgo.utils.ConfigReader;

import io.cucumber.java.en.Given;
import io.cucumber.java.en.Then;

public class DDataParallelSceStep {
/*	DataStructurePage dataStructurePage;
	HomePage homePage;
	CommonSteps commonSteps;
	WebDriver driver;
    //ConfigReader configReader;

	@Given("open {string} with url")
	public void open_with_url(String string) throws Exception {
		DriverFactory driverFactory = new DriverFactory();
		driverFactory.SingleDriver(string);
 
		String url = ConfigReader.getBaseUrl();

		DriverFactory.getDriver().get(url);
		DriverFactory.getDriver().manage().window().maximize();
		DriverFactory.getDriver().manage().timeouts().implicitlyWait(Duration.ofSeconds(4));  
	}
*/
/*	@Given("ClickOnHomePage {string}")
	public void click_on_home_page(String string) {
	}
*/
}
